package main.java.com.cappataz.gui;

import javax.swing.*;

public class GestionarUsuariosFrame extends JFrame {
    // Código para gestionar usuarios (CRUD)
    public GestionarUsuariosFrame() {
        setTitle("Gestionar Usuarios");
        setSize(500, 400);
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        setLocationRelativeTo(null);
        // Añadir componentes y lógica para gestionar usuarios
    }
}
